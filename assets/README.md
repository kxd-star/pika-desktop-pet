# Optional Local Artwork

Place a transparent PNG at:

```text
assets/pikachu.png
```

The desktop pet will prefer this local asset. When it is absent, the application attempts to load the Pikachu artwork URL documented in `ATTRIBUTION.md`, then falls back to the built-in vector-style drawing.
